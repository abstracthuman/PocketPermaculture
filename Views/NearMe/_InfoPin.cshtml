<script id="pin-user-template" type="x-tmpl-mustache">
    <h3 class="pin-user">Pins by: {{userName}}</h3>
</script>

<script id="pin-template" type="x-tmpl-mustache">
    <div class="pin">
        <div class="card {{category}} {{type}}">
            <h5 class="card-header">{{title}}</h5>
            <div class="card-body">
                <p class="card-text">{{description}}</p>
            </div>
        </div>
    </div>
</script>